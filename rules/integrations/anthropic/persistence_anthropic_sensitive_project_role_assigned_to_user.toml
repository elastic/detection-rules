[metadata]
creation_date = "2026/09/15"
integration = ["anthropic"]
maturity = "production"
updated_date = "2026/09/15"

[rule]
author = ["Elastic"]
description = """
Detects when a Claude project owner or editor role is granted through a `role_assignment_granted` event. Project owners
and editors can access project chats, artifacts, and knowledge bases that may hold sensitive data. An attacker with
organization access can grant these roles to persist access to high-value project content without holding organization
admin privileges. This is a New Terms rule that alerts when the assigner (`user.id`) has not granted one of these
project roles in the history window. Project creators often assign themselves owner; treat first-seen assigner activity
plus the affected project as the signal.
"""
false_positives = [
    """
    Legitimate project collaboration routinely adds owners and editors when teams spin up or staff Claude projects.
    Verify the assigner (`user.id`), project (`anthropic.audit.resource_id`), and role against expected membership
    changes.
    """,
    """
    Shared service or automation accounts may receive editor access during content workflows. Confirm the assigner
    and affected project are expected for the workflow; grantee identity may require correlating org membership or
    `anthropic.audit.target_id` when the API supplies it.
    """,
]
from = "now-9m"
index = ["logs-anthropic.audit-*"]
language = "kuery"
license = "Elastic License v2"
name = "Anthropic Sensitive Claude Project Role Assigned to User"
note = """## Triage and analysis

### Investigating Anthropic Sensitive Claude Project Role Assigned to User

This is a first-seen (new_terms) signal keyed on **`user.id`** (the assigner/actor) for `role_assignment_granted`
events where `user.target.roles` is `chat_project:owner` or `chat_project:editor`. Organization-wide `admin` grants
use the same action with `user.target.roles` equal to `admin` and are covered by **Anthropic Admin Role Assigned to
User**. Admin grants through `rbac_role_assigned` use `anthropic.audit.role_id` instead of `user.target.roles` and are
not matched here.

The rule tracks first-seen **assigners** granting sensitive project roles, not first-seen grantees. Self-assignment at
project creation is common—treat novelty of the actor plus project context as the signal. When a grantee is present,
review **`user.target.id`**, **`anthropic.audit.target_id`**, **`anthropic.audit.target_type`**, and **`related.user`**.

#### Possible investigation steps

- Identify the assigner using `user.id`, `user.email`, `source.ip`, and `user_agent.original` when
  `anthropic.audit.actor.type` is `user_actor`.
- Review `user.target.roles`, `anthropic.audit.resource_type`, `anthropic.audit.resource_id`, and
  `anthropic.audit.actor.type` for the affected Claude project.
- If grantee context is needed, inspect `anthropic.audit.target_id`, `anthropic.audit.target_type`, and
  `related.user`; correlate with org membership changes in the same window.
- Determine whether the assigner or project had other IAM, export, or sharing changes recently.
- Correlate with data export, artifact sharing, or compliance logging changes involving the same project or users.

### False positive analysis

- Confirm with the project or organization administrator that the assignment was intentional.
- Baseline recurring collaborators and internal project onboarding patterns before escalating.

### Response and remediation

- If unauthorized, revoke the project role on the affected resource and review other changes by the same assigner
  (`user.id`).
- Audit project content access and recent exports for the affected resource.
"""
references = [
    "https://platform.claude.com/docs/en/api/compliance/activities/list",
]
risk_score = 47
rule_id = "95976599-37b3-45f3-ac74-a45ccbb1cdf9"
severity = "medium"
tags = [
    "Domain: Cloud",
    "Domain: LLM",
    "Data Source: Anthropic",
    "Use Case: Identity and Access Audit",
    "Use Case: Threat Detection",
    "Use Case: UEBA",
    "Rule Type: New Terms",
    "Resources: Investigation Guide",
    "Tactic: Persistence",
    "Tactic: Privilege Escalation",
]
timestamp_override = "event.ingested"
type = "new_terms"

query = '''
data_stream.dataset: "anthropic.audit" and
    event.category: "iam" and
    event.action: "role_assignment_granted" and
    user.target.roles: ("chat_project:owner" or "chat_project:editor")
'''


[[rule.threat]]
framework = "MITRE ATT&CK"
[[rule.threat.technique]]
id = "T1098"
name = "Account Manipulation"
reference = "https://attack.mitre.org/techniques/T1098/"
[[rule.threat.technique.subtechnique]]
id = "T1098.003"
name = "Additional Cloud Roles"
reference = "https://attack.mitre.org/techniques/T1098/003/"



[rule.threat.tactic]
id = "TA0003"
name = "Persistence"
reference = "https://attack.mitre.org/tactics/TA0003/"
[[rule.threat]]
framework = "MITRE ATT&CK"
[[rule.threat.technique]]
id = "T1098"
name = "Account Manipulation"
reference = "https://attack.mitre.org/techniques/T1098/"
[[rule.threat.technique.subtechnique]]
id = "T1098.003"
name = "Additional Cloud Roles"
reference = "https://attack.mitre.org/techniques/T1098/003/"



[rule.threat.tactic]
id = "TA0004"
name = "Privilege Escalation"
reference = "https://attack.mitre.org/tactics/TA0004/"

[rule.investigation_fields]
field_names = [
    "@timestamp",
    "event.action",
    "event.id",
    "organization.id",
    "user.id",
    "user.target.roles",
    "anthropic.audit.target_id",
    "anthropic.audit.target_type",
    "related.user",
    "anthropic.audit.resource_id",
    "anthropic.audit.resource_type",
    "anthropic.audit.actor.type",
    "user.email",
    "user.target.id",
    "source.ip",
    "user_agent.original",
]

[rule.new_terms]
field = "new_terms_fields"
value = ["user.id"]
[[rule.new_terms.history_window_start]]
field = "history_window_start"
value = "now-14d"
