[metadata]
creation_date = "2026/06/10"
integration = ["anthropic"]
maturity = "production"
updated_date = "2026/06/15"

[rule]
author = ["Elastic"]
description = """
Anthropic Claude GitHub Enterprise integrations reject webhook deliveries when the HMAC signature does not match the
configured secret. This activity event records that validation failure; it does not mean a payload was accepted or that
repository content reached Claude Code Security workflows. Repeated failures may indicate secret mismatch during
maintenance, misconfigured GitHub Enterprise delivery settings, or an adversary probing the endpoint with forged
requests before or during a supply chain attack against connected repositories.
"""
false_positives = [
    """
    GitHub Enterprise configuration changes, secret rotation, or TLS inspection proxies can cause signature validation
    failures during maintenance. Review recent webhook secret updates and infrastructure changes.
    """,
]
from = "now-9m"
index = ["logs-anthropic.audit-*"]
language = "kuery"
license = "Elastic License v2"
name = "Anthropic GitHub Enterprise Webhook Signature Invalid"
note = """## Triage and analysis

### Investigating Anthropic GitHub Enterprise Webhook Signature Invalid

Invalid webhook signatures mean Anthropic rejected a GitHub Enterprise delivery for the configured integration. The
audit event confirms validation failed; triage should focus on whether the failure is expected maintenance or
unexpected probing near integration changes.

#### Possible investigation steps

- Review `anthropic.audit.ghe_configuration_id`, `anthropic.audit.actor.type`, and `organization.id`.
- When the actor is a `user_actor`, review `user.email`, `user.id`, `source.ip`, and `user_agent.original` as the
  administrative session tied to the audit record. These fields do not identify the webhook delivery source IP.
- Search for recent `ghe_configuration_created`, `ghe_configuration_updated`, or
  `claude_code_security_webhook_secret_updated` events for the same configuration.
- Confirm with the GitHub Enterprise administrator whether secrets or endpoints changed.
- Review GitHub audit logs for unexpected webhook or repository configuration changes.

### False positive analysis

- Signature failures during secret rotation are common if both sides were not updated at the same time.

### Response and remediation

- Rotate webhook secrets if compromise is suspected and verify only trusted repositories remain connected.

## Setup

The Anthropic Fleet integration, or similarly structured data from the Anthropic compliance activities API, is
required to be compatible with this rule."""
references = [
    "https://attack.mitre.org/techniques/T1195/",
    "https://docs.github.com/en/webhooks/using-webhooks/validating-webhook-deliveries",
    "https://platform.claude.com/docs/en/api/compliance/activities/list",
]
risk_score = 47
rule_id = "14290b38-a8dd-474d-bfd7-cce282f4f745"
severity = "medium"
tags = [
    "Domain: Cloud",
    "Domain: LLM",
    "Data Source: Anthropic",
    "Use Case: Threat Detection",
    "Resources: Investigation Guide",
    "Tactic: Initial Access",
]
timestamp_override = "event.ingested"
type = "query"

query = '''
data_stream.dataset: "anthropic.audit" and
    event.category: "configuration" and
    event.action: "ghe_webhook_signature_invalid"
'''


[[rule.threat]]
framework = "MITRE ATT&CK"
[[rule.threat.technique]]
id = "T1195"
name = "Supply Chain Compromise"
reference = "https://attack.mitre.org/techniques/T1195/"


[rule.threat.tactic]
id = "TA0001"
name = "Initial Access"
reference = "https://attack.mitre.org/tactics/TA0001/"

[rule.investigation_fields]
field_names = [
    "@timestamp",
    "event.action",
    "event.id",
    "organization.id",
    "anthropic.audit.ghe_configuration_id",
    "anthropic.audit.actor.type",
]

