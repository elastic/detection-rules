[metadata]
creation_date = "2026/08/03"
maturity = "production"
updated_date = "2026/08/03"

[rule]
author = ["Elastic"]
description = '''
Detects list operations on Kubernetes Secrets from a non-loopback client when the request URI targets cluster-wide
secrets or list operations under kube-system or default. Useful for spotting broad secret enumeration from remote
clients.
'''
from = "now-6m"
index = ["logs-aws_logs.eks_audit-*"]
language = "kuery"
license = "Elastic License v2"
name = "AWS EKS - Kubernetes Secrets List Across Cluster or Sensitive Namespaces"
note = '''
## Triage and analysis

### Investigating AWS EKS - Kubernetes Secrets List Across Cluster or Sensitive Namespaces
Audit events for `list` on the `secrets` resource against `/api/v1/secrets`, paginated cluster lists, or namespace-scoped
lists under `kube-system` or `default`, from a source IP that is not localhost.

### Investigation steps

- Confirm the actor (`user.name`, groups) and whether the client is expected (CI, admin bastion, controller).
- Review `aws.eks.audit.requestURI`, `user_agent.original`, and follow-on API activity from the same source.
- Assess exposure: cluster-wide secret listing can surface many credentials.

### False positives

- Legitimate controllers or operators listing secrets in `kube-system` / `default` from cluster nodes may match; tune by
  source IP, user agent, or service account as needed.'''
references = ["https://attack.mitre.org/techniques/T1552/007/"]
risk_score = 73
rule_id = "a4734e68-ae02-53da-892d-5403beceb95f"
setup = '''
This rule reads EKS control-plane audit logs collected by the Custom AWS Logs
integration into `logs-aws_logs.eks_audit-*`, where the AWS-native EKS ingest
pipelines normalize each audit record under `aws.eks.audit.*`.

Enable the `audit` control-plane log type on the EKS cluster, send its
CloudWatch log group through the Custom AWS Logs integration, and confirm
events carry `data_stream.dataset: aws_logs.eks_audit` and
`cloud.provider: aws` before enabling this rule.
'''
severity = "high"
tags = [
    "Data Source: Kubernetes",
    "Domain: Kubernetes",
    "Use Case: Threat Detection",
    "Tactic: Credential Access",
    "Tactic: Discovery",
    "Resources: Investigation Guide",
    "Domain: Cloud",
    "Data Source: AWS",
]
timestamp_override = "event.ingested"
type = "query"
query = '''
event.dataset:"aws_logs.eks_audit" and event.action:list and
aws.eks.audit.objectRef.resource:secrets and
aws.eks.audit.requestURI :(/api/v1/secrets or /api/v1/secrets?limit* or /api/v1/namespaces/kube-system/secrets or /api/v1/namespaces/kube-system/secrets?limit* or /api/v1/namespaces/default/secrets or /api/v1/namespaces/default/secrets?limit*) and
source.ip:(* and not ("::1" or "127.0.0.1")) and
not user.name: (system\:kube-controller-manager or eks\:cloud-controller-manager or eks\:kms-storage-migrator) and
not aws.eks.audit.user.groups:"system:serviceaccounts:ibm-csi"
'''

[[rule.threat]]
framework = "MITRE ATT&CK"

[rule.threat.tactic]
id = "TA0006"
name = "Credential Access"
reference = "https://attack.mitre.org/tactics/TA0006/"

[[rule.threat.technique]]
id = "T1552"
name = "Unsecured Credentials"
reference = "https://attack.mitre.org/techniques/T1552/"

[[rule.threat.technique.subtechnique]]
id = "T1552.007"
name = "Container API"
reference = "https://attack.mitre.org/techniques/T1552/007/"

[[rule.threat]]
framework = "MITRE ATT&CK"

[rule.threat.tactic]
id = "TA0007"
name = "Discovery"
reference = "https://attack.mitre.org/tactics/TA0007/"

[[rule.threat.technique]]
id = "T1613"
name = "Container and Resource Discovery"
reference = "https://attack.mitre.org/techniques/T1613/"
